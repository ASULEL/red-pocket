server:
  port: 8888

spring:
  application:
    name: boot-red-pocket

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/redpocket?useSSL=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

mybatis-plus:
  type-aliases-package: com.asule.redpocket
  mapper-locations:  classpath:mapper/*.xml

